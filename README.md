# reachinbox-email-scheduler
Production-grade email scheduler with SMTP sending, background workers, retries, and delivery tracking.

# init backend 
 mkdir backend
 cd backend
 npm init -y
 npm install express dotenv cors nodemailer

#init frontend
 mkdir frontend
 cd frontend
 npm init -y
 npm install react react-dom axios
